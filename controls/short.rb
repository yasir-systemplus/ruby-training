# count = 2

# message = "Hello Word" if count > 1

# puts message

# puts if count >= 1 ? "Hello to #{count}" : "Hello World"


DEFAULT_SPEED = 50 

# limit ||= DEFAULT_SPEED

# puts limit

# limit = nil

# limit += DEFAULT_SPEED + 10 unless limit

# puts limit

score = 5 

score += 10 unless score >= DEFAULT_SPEED

puts score